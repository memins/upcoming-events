extends layout
block content

        h2 Users
        p There are #{users.length} users.
        
        ol 
            each user in users
                li 
                    a(href=`/users/${user.id}`)= user.name